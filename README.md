Advent of Code 2025











Notes:

day2 pseudo

convert input to array of arrays or array of strings?
loop over



// the second loop from the first part of day2 problem

// for (let i = start; i <= end; i++) {
// 			let splitIndex = Math.ceil(i.toString().length / 2)
// 			let cHalf = i.toString().slice(0, splitIndex)
// 			let dHalf = i.toString().slice(splitIndex)
// 			let finalNum = Number(`${Number(cHalf)}${Number(dHalf)}`)
// 			cHalf === dHalf ? (total += finalNum) : null
// 		}
